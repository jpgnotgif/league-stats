extends layout

block content
    div
      form(action="/summoner" method="POST", class="form-horizontal")
        .form-group
          .row
            input(class="form-control", name="name", type="text", placeholder="Enter a summoner name")
        .form-group
          .row
            button(type="submit", class="btn btn-default") Summon!
